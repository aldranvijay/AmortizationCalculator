To run the Amortization UI application(for demo)

Steps:
1. Open terminal into current directory
2. Run command "npm install"
3. Once all dependent node_modlues downloaded, start application using "npm start" command.
4. Open browser and URL "http://localhost:8081/"

Note : This application will only be showing data when Spring Boot Microservice "AmortizationCalc" will already running.